type LimitOrderRequest = record {};
type LimitOrderResponse = record { order_id : nat64 };
type OrderStatus = variant { NotFound; Pending };

service : {
    add_limit_order : (LimitOrderRequest) -> (LimitOrderResponse);
    get_order_status : (nat64) -> (OrderStatus) query;
}
