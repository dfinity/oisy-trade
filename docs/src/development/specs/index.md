# Specs

Specs live one-per-ticket as `DEFI-XXXX-short-slug.md` and follow the [template](template.md). Each spec drives a spec-driven build loop (implementer → reviewer → ready) — see the project's `CLAUDE.md` for the loop itself.
