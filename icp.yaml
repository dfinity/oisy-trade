canisters:
  - name: oisy_trade
    recipe:
      type: "@dfinity/rust@v3.0.0"
      sha256: 620151f0c07efc1e8a986f73b85406b78bea09a92fc899f299a431431a6a6819
      configuration:
        package: oisy_trade_canister
        candid: canister/oisy_trade.did
        shrink: true
        compress: true

environments:
  - name: staging
    network: ic
    canisters: [oisy_trade]
    init_args:
      oisy_trade: "( variant { Init = record { mode = variant { GeneralAvailability }; max_orders_per_chunk = 1_000 : nat32; instruction_budget = 1_000_000_000 : nat64; max_fills_per_settling_event = opt (128 : nat32) } } )"

  - name: prod
    network: ic
    canisters: [oisy_trade]